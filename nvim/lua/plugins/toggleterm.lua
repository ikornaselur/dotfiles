require("toggleterm").setup{
  size = 12,
  open_mapping = [[<c-\>]],
  hide_numbers = true,
  direction = 'float',
  float_opts = {
    border = 'single',
    width = 100,
    height = 35,
    winblend = 3,
  }
}

